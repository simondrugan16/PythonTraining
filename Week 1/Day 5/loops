list_of_objects = ["item", "thing", "dohickey", "mjiggy", "mbob"]

for obj in list_of_objects:
    print(obj)

list_of_nums = [555, 1, 5, 100]
print(f"Inbuilt sum method: {sum(list_of_nums)}")

iterative_sum = 0
for num in list_of_nums:
    iterative_sum += num
print(f"Custom sum method: {iterative_sum}")

iterative_max = list_of_nums[0]
for num in list_of_nums:
    if num > iterative_max:
        iterative_max = num
print(f"Custom max method: {iterative_max}")

print(f"Odd nums")
for num in range(1, 10, 2):
    print(f"Num in range is {num}")
